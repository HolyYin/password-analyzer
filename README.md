# password-analyzer
Password Analyzer written in Python that checks password strength using length rules, character requirements, repeated-character detection, non-ASCII validation, and a common-password wordlist. It provides real-time feedback and suggestions to help users create stronger passwords.
